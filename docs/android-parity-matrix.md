# Android parity matrix

This is the versioned contract index for the native Android client. The web app remains the behavioral source of truth. A row may move to **ready** only when its API shape, RLS expectation, Android route, and executable test fixture are linked here.

| Domain | Android release behavior | Web sources | Backend/API dependency | Android surface | Required verification | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Authentication | Restore a session, passkey sign-in, invite redemption, sign-out, and actionable errors. | `apps/web/src/lib/auth.ts`, `InviteRedeemForm.tsx` | Supabase Auth; `redeem-invite`; Credential Manager/App Link association. | Auth graph | Existing-account device proof; invite error matrix; token clearing test. | Discovery |
| Library | Read cached library; poster/list views; filters, sort, and title navigation. | `Library.tsx`, `useAppStore.ts`, `fetchUserLibrary()` | `titles`, seasons/episodes relations; owner RLS; incremental cursor contract. | Library | Contract fixture; Room mapping; compact/expanded screenshot; offline read. | Contract documented ([library.md](./android-contracts/library.md)); Room mapping/screenshot tests pending. **Remove from library landed**: Title detail screen's confirm-then-delete action mirrors `useAppStore.ts`'s `removeTitle` — `LibraryRepository.removeTitle` hard-deletes locally (Room `on delete cascade` takes seasons/episodes/viewings/cast/crew/outings with it) and enqueues a `title`/`delete` outbox entry; `SupabaseRemoteMutationWriter.deleteTitle` pushes the server-side delete, which fans out through schema.sql's own cascades and `sync_tombstones` for any other synced device. |
| Title detail | Read film/TV metadata, credits, history, status, notes, and external watch link. | `TitleDetailDrawer.tsx`, `db.ts` | Title/details relation shape; media image allowlist. | Title detail | Detail fixture; read-only RLS test; deep-link test. | Contract documented ([title-detail.md](./android-contracts/title-detail.md)); deep-link test pending |
| Episode tracking | Keep watches, ratings, and reviews independent; present correct rollups. | `episodeUtils.ts`, `logEpisodeToDb()` | `episodes`, watch/rating/review tables; idempotent atomic command. | Title detail | Shared rollup fixtures; offline/retry/process-death tests. | Contract documented ([episode-tracking.md](./android-contracts/episode-tracking.md)); idempotency gap flagged, offline/retry tests pending |
| Discover/add | Search/browse through the protected media proxy; explain duplicates. | `AddTitleWorkflow.tsx`, `media.ts` | `media-proxy` actions; rate limits; TMDB attribution/cache rules. | Discover | Proxy contract fixture; offline/error UI tests. | **Read and add both landed.** Discover's grid calls the real `media-proxy` `trending` action (`DiscoverRepository`, an OkHttp GET against `/functions/v1/media-proxy` via `SupabaseRestClient.invokeFunction`, mirroring `apps/web/src/lib/media.ts`'s `fetchTrending('all')` interleave of movie/TV pages) with loading/error/retry states — the anon key backstops the bearer token when signed out, since Discover browsing shouldn't require sign-in. **Adding is now a real write**, replacing the `SampleCatalog.kt`/`DiscoverSampleStore` mock (both deleted): `DiscoverRepository.searchMedia`/`fetchDetails` cover the `search`/`details`/`season`/`ratings` actions, the TMDB→domain mapping lives in `MediaProxyParsing.kt` (pure, unit-tested against recorded payload shapes in `MediaProxyParsingTest`, 14 cases, tracking `media.ts`'s `mapSearchItem`/`fetchMediaDetails`/`extractCertification`/`mapTmdbCast` field for field), and `LibraryRepository.addTitle` writes the title plus its seasons, episodes, top-billed cast, key crew and — for a WATCHED add — a seed viewing to Room, enqueuing **one** `title`/`insert` outbox entry carrying the whole graph (`SupabaseRemoteMutationWriter.insertTitle` orders the child inserts itself; separate entries could push a season before its title). Duplicates are refused against the local mirror of the server's own `unique_user_tmdb` constraint, with an "open the copy you have" affordance. Covered by `LibraryRepositoryAddTitleTest` (11 cases). **Verified on a physical device** (2026-07-25) end to end, including the full round trip back down into a second install via Supabase — see `docs/android-implementation-status.md` for the account, and for the two bugs that pass found and fixed. Still gapped vs. the web app: no tags, no `rt_link`/`accolades` (Wikidata) enrichment, no per-season/per-episode cast or crew, no watched-date picker (a WATCHED add dates the seed viewing today), and no "got tickets?" hand-off into the outing scheduler. |
| Up Next | Show upcoming titles and next unwatched episodes. | `UpNext.tsx`, `store/upNext.ts` | Library bootstrap fields and calculation version. | Up Next | Shared derivation fixtures; empty/error states. | Implemented against real local data: a new Up Next tab derives "continue watching" progress by aggregating `SeasonEntity` counts per WATCHING title (`LibraryRepository.observeUpNext()`), with a watchlist section below. "Mark episode watched" advances the next unwatched `EpisodeEntity` row but doesn't touch the title's status or the season's stored `episodesWatched` aggregate (that field isn't recomputed locally anywhere in the app; matches existing Title Detail behavior). No calculation-version/bootstrap-field contract with the web app yet. |
| Ledger | Present all existing calculations and widget preferences with accessible alternatives. | `ledgerDerive.ts`, `ledgerStats.ts`, `ledgerPanels.ts` | Stable library/history inputs; `user_prefs.ledger_layout`. | Ledger | Shared calculation fixtures; widget persistence; chart semantics. | Contract documented ([ledger.md](./android-contracts/ledger.md)); all 20 widgets implemented (hero ribbon + full board), each a fixed-400dp card with a genuine accessible list alternative per §5 — not the web app's tooltip-only fallback. `user_prefs.ledger_layout` now pulls on sign-in and app launch, not just push, honoring ledger.md §4's "server wins on load" rule (`LedgerLayoutRepository.reconcile()`). `timeRange`/`scope` widget settings are consumed by `LedgerRepository.observeLedgerBoards()` (per-widget-instance, allowlist-gated by panel via `PANEL_SETTING_KEYS`/`effectiveLedgerSettings()` in `core:model`), with a per-widget settings sheet (`Scope`/`Time range` segmented controls, shown only for panels that honor them) and panel-default `topN` caps matching the web app. Editor gained capability parity: long-press drag-reorder and drag-resize (existing up/down buttons and tap-to-cycle width kept as fallbacks), duplicate, "Reset to default layout" with confirmation, and an "Add a widget" palette with live scaled previews, a "×N already on board" usage badge, and long-press-drag-to-place. Chart primitives upgraded: a true daily 7×52 heatmap (`DailyHeatmapGrid`), line charts for Shifting Standards/Premieres & Revivals (`LineChartCanvas`), and a 30-night grid for The Marathon — Screening Nights stays a bar chart (no radar; no accessible-parity benefit). Hero row gained a "now showing · {date}" kicker, a narrative sentence, and 2 more stat tiles (6 total: Movies/Series/Screenings/Hours logged/Days in the dark/Avg rating). `LedgerRepositoryTest` (`data` module, 27 cases) covers all 20 widgets against the same fixture `docs/android-implementation-status.md`'s Phase 3 section hand-verified on-device, closing this row's "Shared calculation fixtures" requirement; `LedgerPanelSettingsTest`/`LedgerLayoutRepositoryTest` cover the settings-resolution and layout-reconciliation logic respectively. Implemented per [`docs/superpowers/plans/2026-07-23-android-ledger-parity.md`](../superpowers/plans/2026-07-23-android-ledger-parity.md). **Verified live on a physical device** (2026-07-24, see `docs/android-implementation-status.md`'s Phase 3 section for the full account): drag-reorder, drag-resize, duplicate, remove, add-from-palette, the usage badge, and the reset-confirmation dialog all confirmed working; the settings sheet's `Scope`/`Time range` controls confirmed to actually filter rendered widget data, not just persist; `DailyHeatmapGrid`/`LineChartCanvas`/the 30-night grid all confirmed rendering correctly against real data; layout changes confirmed to survive a full force-stop. That pass also found and fixed a real bug: `CategorySection`-based widgets (Feature Lengths, On the Air, By the Era, Critical Record, By the Genre, The Auteurs, The Ensemble, In Translation, The Revival House) rendered as a completely blank card — no title, no message — when empty, unlike every other widget's empty state; now always shows a header plus a tailored empty message. Friend/shared Ledger viewing remains out of scope, gated on the Friends/Sharing rows below (§8 of the plan pre-plans the hook: a read-scope parameter on `LedgerRepository`, `moviegoing`'s per-field owner-only degradation, and a read-only board for that context). At the Movies now also breaks total spend down per venue and per format (`LedgerMoviegoingStats.venueSpend`/`formatSpend`) and surfaces the cheapest-per-trip venue among those with 2+ priced trips (`bestValueVenue`) — Android-exclusive, no web equivalent (issues #201/#210/#212). These inherit the same owner-private degradation as `formats`/`totalSpend` once a friend/shared Ledger view exists. |
| Cinema outings | Schedule/edit/cancel a cinema trip; auto-complete (watched + viewing logged with venue/companions) once `showtime + previews + runtime` passes; post-show rate/note/"didn't make it". | `apps/web/src/store/outings.ts`, `apps/web/src/components/OutingScheduleSheet.tsx`, `apps/web/src/components/PostShowSheet.tsx`, `complete_due_outings()` | `cinema_outings`; owner-only RLS; `viewings.venue/companions/outing_id`. | Title detail (CTA/banner/stub), Up Next ("On the Marquee" / "Fresh from the lobby"), Library (🎟 badge) | Countdown-bucket + completion-idempotency unit fixtures; on-device exact-alarm/notification verification. | **Backend wiring landed.** Full CRUD + a Kotlin-side completion engine (`OutingsRepository.completeDueOutings`, deduped via the viewing's `outingId` rather than a DB transaction) reconciled on app launch/resume and a real `AlarmManager` exact alarm + system notification (re-armed across reboot via a `BOOT_COMPLETED` receiver) — the one place Android exceeds the web app, which can't complete while its tab is closed. `SupabaseRemoteMutationWriter`'s `cinema_outing` upsert case now routes through the real writer DI'd in `CinemArchiveApplication` (same as every other domain, since Authentication and the real outbox both landed). The read half — `sync_library_changes` had no `union all` arm for `cinema_outings` and the `viewing` arm never carried `companions`/`outing_id` — was closed in `supabase/migrations/20260722000000_cinema_outings_sync.sql` plus `LibrarySyncRepository`'s new `cinema_outing` upsert/tombstone case, so outings and their derived viewings now round-trip both ways. In-app plan sharing and the `outing_completed`/`outing_plans_shared` notification-inbox types remain deferred: gated on Friends/social leaving Discovery. `OutingScheduleSheet`'s venue and companions fields now autocomplete from the user's own outing history (`OutingsRepository.observeVenueSuggestions()`/`observeCompanionSuggestions()`, distinct/most-recent-first over local `cinema_outings` rows — issues #197/#198), and a per-venue parking/transit notes field (`venue_notes` local table, `VenueNoteDao`) pre-fills from the last visit to that venue (issue #214). Local-only (no Supabase table backs `venue_notes` yet); web has neither autocomplete nor per-venue notes. See `docs/superpowers/plans/2026-07-21-android-cinema-outings.md`. |
| Sharing | Create/revoke/expire scoped, read-only share links and open shared libraries. | `Profile.tsx`, `auth.ts`, `fetchSharedLibrary()` | `shared_access_keys`, `share_scopes`, shared-token RPC, RLS. | Profile / shared library | Anonymous/valid/expired/revoked/scope RLS matrix; App Link routes. | Discovery |
| Friends and social | Friend states, friend libraries, comments, reactions, activity, recommendations. | `Friends.tsx`, `db.ts`, social components | Friendship state machine; social tables/triggers; scoped friend RLS. | Friends | Owner/friend/blocked/scoped fixtures; pagination and mutation errors. | Discovery |
| Notifications | In-app inbox and, later, privacy-safe FCM routes. | `NotificationCenter.tsx`, notification store actions | `notifications`; canonical event dispatch; device installation endpoint. | Notification center | Read/dismiss fixture; cold-start route test; lock-screen redaction review. | Discovery |
| Lists | Create/rename/delete custom lists; add/remove a title to/from any number of lists, independent of watch status. Private to the owner. | `apps/web/src/views/Lists.tsx`, `AddToListSheet.tsx`, `useAppStore.ts`'s `ListsSlice`, `db.ts` | `lists`, `list_items`; owner-only RLS; `sync_library_changes` `list`/`list_item` arms. | New "Lists" tab (`feature:lists`); "Add to list" sheet off Title detail (`core:designsystem`'s `AddToListSheet`). | Contract fixture ([lists.md](./android-contracts/lists.md)); Room mapping; on-device sync round-trip with web. | Implemented against real Room + outbox data, mirroring `LibraryRepository.updateTitleStatus`'s optimistic-write shape: `ListsRepository` (Room write first, then outbox), `SupabaseRemoteMutationWriter`'s `list`/`list_item` upsert/delete cases, and `LibrarySyncRepository`'s two new pull arms (`SYNC_SCHEMA_VERSION` bumped to 6; `list_item` is the first entity held back on **two** independent parents in `DeferredRows`, not one). `./gradlew :app:assembleDebug :app:lintDebug testDebugUnitTest` all pass. **Not yet verified on a physical device** — no on-device cross-client sync pass or `ListsRepository`/sync-arm unit tests have been added yet; see this row's own TODO before marking ready. |
| Preferences and themes | Persist four themes plus navigation/Ledger preferences. | `theme.ts`, `Profile.tsx`, `saveLedgerLayout()` | `user_prefs`; local DataStore policy. | Profile/settings | Theme token screenshots; account-switch clearing test. | Appearance now lives in its own overlay off a Profile overlay (avatar, stats, sign-out placeholder), reached instead of a gear-icon Settings screen. Theme is two independent DataStore-backed prefs rather than one four-way enum: `ArchiveThemeMode` (System/Light/Dark) × `ArchivePalette` (Brand, Material You dynamic color via `dynamicDarkColorScheme`/`dynamicLightColorScheme`, and the unlockable Spider-Noir/Matrix palettes, which ignore theme mode). About & Legal is a separate overlay (version, credits, GitHub links, placeholder Terms/Privacy/Credits, each opening as its own sub-page). Ledger-layout persistence unchanged. Still no `user_prefs` sync; navigation preference persistence and account-switch clearing pending |

## Contract completion rules

Every row must document the following before its first Android write is enabled:

1. Request and response fields, IDs, enum values, timestamps, pagination, and error codes.
2. The RLS authorization matrix, including the unauthorized direct-REST case.
3. Whether the action is idempotent, its operation ID format, and retry/conflict behavior.
4. JSON fixture files derived from production-compatible test data and a test that runs them on both web and Android logic where applicable.
5. An Android route and its loading, empty, offline, and recoverable-error states.

## Immediate contract tasks

- [x] Define the authenticated bootstrap and incremental-sync endpoint/cursor strategy; direct full-library reads are not sufficient for offline sync. See [`docs/android-sync-contract.md`](./android-sync-contract.md) §2 — design draft, not yet implemented as a migration.
- [x] Define server-authoritative `updated_at` and tombstone behavior for every synchronizable entity. See [`docs/android-sync-contract.md`](./android-sync-contract.md) §3 — design draft; adds `updated_at` to 10 tables currently missing it and a new `sync_tombstones` table, none applied yet.
- [x] Add idempotency and atomic-command contracts for title, viewing, episode, social, and share mutations before those Android writes are implemented. See [`docs/android-sync-contract.md`](./android-sync-contract.md) §4 for the general contract and [`docs/android-contracts/`](./android-contracts/) for the Library/Title-detail/Episode-tracking specifics; social/share domains still at Discovery (out of scope for this pass).
- [ ] Validate Credential Manager/WebAuthn relying-party and Digital Asset Links on a physical Android device.
- [ ] Add a Supabase test project plus owner/friend/blocked/share-token RLS fixtures.
